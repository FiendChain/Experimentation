# This will behave similarly to a header file
import sequences as sequence
import math
import complex

# Custom functions
moduleList = ['sequence', 'math', 'complex']
def help():
    print('List of modules are:')
    for module in moduleList:
        print('- %s' %(module)) 
    